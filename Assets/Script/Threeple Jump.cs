using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ThreepleJump : MonoBehaviour
{
    float jumpForce = 600f, speed = 5f;
    float moveX;
    Rigidbody2D rigid;

    bool ThreepleJumpState = false;
    bool isGround = false;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
